<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity" >
    <item android:id="@+id/action_example"
        android:title=""
        android:icon="@android:drawable/ic_menu_add"
        app:showAsAction="never" />
    <!--
    <item android:id="@+id/action_bar_comment_attachment"
        android:title=""
        android:icon="@android:drawable/ic_menu_set_as"
        app:showAsAction="never">
        <menu>
            <item
                android:id="@+id/comment_attachment_photo"
                android:title="Photo" />
            <item
                android:id="@+id/comment_attachment_video"
                android:title="Video" />
            <item
                android:id="@+id/comment_attachment_audio"
                android:title="Audio" />
            <item
                android:id="@+id/comment_attachment_doc"
                android:title="Doc" />
        </menu>
    </item>
    -->
</menu>
